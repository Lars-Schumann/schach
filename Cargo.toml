[package]
name = "schach"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = [
    "alloc",
    "serde_derive",
], default-features = false, optional = true }

[dev-dependencies]
owlchess = "0.4.0"
oorandom = "11.1.5"

[features]
serde = ["dep:serde"]
